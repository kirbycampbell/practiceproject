<h1><%= @user.username %>'s Posts:</h1>

<ul>
  <% @posts.each do |post| %>
    <h3><%= post.title %></h3>
    <p><%= post.content %></p>
    <p>Created: <%= post.created_at %></p>
  <% end %>
</ul>

<%= link_to "New Post", new_user_post_path(@user) %>
